'use strict';

function converter(sum) {
  let x = sum;
  x = x * 1.2 * 73;

  return x;
}

console.log(converter(10));




const converter1 = (sum2) => sum2 = sum2 * 1.2 * 73;

console.log(converter1(100));

// Два варианта я для себя сделал чтобы лучше понять.